import javax.swing.*;
import java.io.*;
import java.text.*;

/**
 * Write your own Input class
 */

public class MyInput {
	/**
	 * Read an int entered by the user in the terminal window If the input is
	 * invalid, the method should ask for the input again
	 * 
	 * @param prompt
	 *            the prompt message displayed to the user
	 */
	public int readInt(String prompt) {
		// Create a stream able to read strings from the terminal
		// (System.in can't: it just reads bytes)

		// Display the prompt

		// Read the input from the user (as a String)

		// Parse the string

		// try again if bad input

	}

	/**
	 * Read an int entered by the user in a dialog box If the input is invalid,
	 * the method should ask for the input again
	 * 
	 * @param prompt
	 *            the prompt message displayed to the user
	 */
	public int readIntDialog(String prompt) {
		// Use a dialog box

		// Parse the string

		// try again if bad input

	}
}
